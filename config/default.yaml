data:
  train_tickers: [AAPL, MSFT, AMZN, GOOGL, META, JPM, JNJ, BRK-B, TSLA, NVDA, V, UNH, PG, HD, MA, BAC, WMT, XOM, PFE, CVX, NFLX, ORCL, NKE, KO, T, CSCO, MRK, ABT]
  oos_tickers: [SPY, BTC-USD]
  start: "2000-01-01"
  end: "2025-07-01"
  dir: data
  reload: true

model:
  window: 10
  feature_set: S27
  hidden_units: 200

training:
  validation_split: 0.1
  epochs: 200
  batch_size: 64
  patience: 20

evaluation:
  threshold: 0.7

paths:
  models: saved_models
